/*

  open - broad any where use and override 
  public - use but no override anywhere
  internal - default
  fileprivate - cannot see outside of the file
  private - only this struct can use it

  private(set) var sum Int  - getter is public, setter is private

*/